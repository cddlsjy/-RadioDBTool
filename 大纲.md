# RadioDroid 数据库独立下载与导出工具 - 详细修改大纲

## 1. 项目创建与环境配置

### 1.1 新建 Android 项目

- 项目名称：`RadioDBTool`
- 包名：`com.example.radiodbtool`
- 语言：Java（方便直接复用原项目 Java 代码）
- 最低 SDK：API 16（与原 RadioDroid 一致）
- 选择 Empty Activity

### 1.2 添加依赖（`app/build.gradle`）

```gradle
dependencies {
    implementation 'androidx.appcompat:appcompat:1.5.1'
    implementation 'com.google.android.material:material:1.8.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    
    // Room 数据库
    implementation 'androidx.room:room-runtime:2.2.6'
    annotationProcessor 'androidx.room:room-compiler:2.2.6'
    
    // 网络请求
    implementation 'com.squareup.okhttp3:okhttp:3.12.13'   // 兼容 Android 4.x
    
    // JSON 解析
    implementation 'com.google.code.gson:gson:2.9.1'
    
    // 可选：图片加载（用于图标显示，非核心）
    // implementation 'com.squareup.picasso:picasso:2.8'
}
```

### 1.3 添加权限（`AndroidManifest.xml`）

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

***

## 2. 复制并精简原项目核心代码

### 2.1 数据库层（保留完整 Room 结构）

从原项目复制以下文件到新项目的对应包：

- `database/RadioDroidDatabase.java`
- `database/RadioStation.java`
- `database/RadioStationDao.java`
- `database/UpdateTimestamp.java`
- `database/UpdateTimestampDao.java`
- `database/Converters.java`
- `database/CountryCount.java`（暂时保留，用于分类统计，可选）
- `database/LanguageCount.java`（可选）

**修改内容**：

- 统一包名为 `com.example.radiodbtool.database`
- 调整 `RadioDroidDatabase` 的数据库版本和迁移（可简化为直接使用最新版本 14，移除所有历史迁移，但保留 `fallbackToDestructiveMigration()` 以便开发测试）
- 移除 `RadioDroidDatabase` 中的 `TrackHistoryDao` 相关引用（因为不需要歌曲历史记录）

### 2.2 数据模型与网络实体

- 复制 `station/DataRadioStation.java` → 包名改为 `com.example.radiodbtool.station`
- 移除 `DataRadioStation` 中与播放、队列、快捷方式等无关的字段和方法（例如 `playableUrl`, `queue`, `prepareShortcut` 等）
- 保留 `DecodeJson`, `toJson`, `hasValidUuid` 等解析和序列化方法

### 2.3 网络通信与服务器管理

- 复制 `RadioBrowserServerManager.java` → 简化：移除 DNS 自动发现逻辑，改为直接使用用户输入的服务器地址
- 复制 `HttpClient.java` → 保留单例 OkHttpClient
- 复制 `Utils.java` → **大幅精简**，只保留以下方法：
  - `downloadFeed()` – 通用 HTTP GET 请求
  - `downloadFeedFromServer()` – 构建完整 URL 并下载
  - `writeFileCache()`, `getCacheFile()` – 简单缓存（可选）
  - `hasAnyConnection()` – 网络状态检查
  - `parseIntWithDefault()`, `sanitizeName()`
  - 删除所有 UI 相关、播放相关、权限相关的代码

### 2.4 核心同步逻辑

- 复制 `RadioStationRepository.java` → **完全重写**，只保留同步和查询功能
  - 保留单例模式
  - 保留 `RadioStationDao` 和 `tempRadioStationDao`（临时数据库）
  - 移除所有与 `LocalBroadcastManager`, `WorkManager`, `DatabaseUpdateWorker`, 通知相关的代码
  - 移除 `SyncCallback` 接口，自定义一个简单的 `ProgressListener`（只包含 `onProgress`, `onSuccess`, `onError`）
  - 重写 `syncAllStations()` 方法，参数接收服务器基础 URL 和 `ProgressListener`
  - 实现分页拉取：先通过 `json/stats` 获取总电台数，然后分页请求 `json/stations?limit=100&offset=0`
  - 每页数据插入临时数据库，完成后替换主数据库
  - 添加 `getFilteredStations()` 同步方法，支持国家、语言、关键词筛选（基于 `RadioStationDao` 的多条件查询）

***

## 3. UI 界面设计

### 3.1 主界面布局（`activity_main.xml`）

使用 `ScrollView` + `LinearLayout` 垂直布局，包含以下区域：

**1. 服务器设置区域**

- 标签：电台服务器地址
- EditText：`etServerUrl`，默认值 `https://de1.api.radio-browser.info`
- Button：`btnSync`（同步数据库）

**2. 同步进度显示区域**

- ProgressBar：`progressBarSync`（水平样式）
- TextView：`tvSyncStatus`

**3. 筛选与导出区域**

- 标签：筛选条件
- EditText：`etCountry`（国家，留空表示全部）
- EditText：`etLanguage`（语言，留空表示全部）
- EditText：`etKeyword`（关键词，匹配名称/标签）
- Spinner：`spinnerExportFormat`（选项：M3U, CSV, JSON）
- Button：`btnExport`（导出筛选结果）
- ProgressBar：`progressBarExport`（不确定样式）
- TextView：`tvExportStatus`

### 3.2 字符串资源

在 `res/values/strings.xml` 中添加所有界面上使用的字符串。

### 3.3 主题与样式

复用原项目的主题色，或者使用简单的 Material Design 主题。

***

## 4. 核心功能实现

### 4.1 同步数据库按钮逻辑（`MainActivity`）

```java
btnSync.setOnClickListener(v -> {
    String serverUrl = etServerUrl.getText().toString().trim();
    if (serverUrl.isEmpty()) {
        Toast.makeText(this, "请输入服务器地址", Toast.LENGTH_SHORT).show();
        return;
    }
    // 保存到 SharedPreferences
    getSharedPreferences("settings", MODE_PRIVATE).edit()
        .putString("server_url", serverUrl).apply();
    
    btnSync.setEnabled(false);
    progressBarSync.setIndeterminate(true);
    tvSyncStatus.setText("正在同步，请稍候...");
    
    new Thread(() -> {
        repository.syncAllStations(this, serverUrl, new ProgressListener() {
            @Override
            public void onProgress(String message, int current, int total) {
                runOnUiThread(() -> {
                    if (total > 0) {
                        progressBarSync.setIndeterminate(false);
                        progressBarSync.setMax(total);
                        progressBarSync.setProgress(current);
                        tvSyncStatus.setText(message + " " + current + "/" + total);
                    } else {
                        tvSyncStatus.setText(message);
                    }
                });
            }
            @Override
            public void onSuccess(String message) {
                runOnUiThread(() -> {
                    tvSyncStatus.setText(message);
                    btnSync.setEnabled(true);
                    Toast.makeText(MainActivity.this, message, Toast.LENGTH_LONG).show();
                });
            }
            @Override
            public void onError(String error) {
                runOnUiThread(() -> {
                    tvSyncStatus.setText("同步失败：" + error);
                    btnSync.setEnabled(true);
                    Toast.makeText(MainActivity.this, "同步失败：" + error, Toast.LENGTH_LONG).show();
                });
            }
        });
    }).start();
});
```

### 4.2 筛选导出逻辑

- 导出按钮点击后，收集筛选条件（国家、语言、关键词）
- 在后台线程调用 `repository.getFilteredStations(country, language, keyword)`
- 若结果为空则提示；否则调用 Android 存储访问框架（SAF）让用户选择保存文件
- 根据选择的格式调用 `ExportHelper` 写入文件

**ExportHelper 实现**（新建类）：

```java
public class ExportHelper {
    public static void exportToM3U(List<RadioStation> stations, OutputStream out) throws IOException {
        BufferedWriter writer = new BufferedWriter(new OutputStreamWriter(out));
        writer.write("#EXTM3U\n");
        for (RadioStation s : stations) {
            writer.write("#EXTINF:-1," + s.name + "\n");
            writer.write(s.url + "\n\n");
        }
        writer.flush();
    }
    // 类似实现 CSV 和 JSON
}
```

### 4.3 数据库筛选方法（在 `RadioStationDao` 新增）

```java
@Query("SELECT * FROM radio_stations " +
       "WHERE (:country = '' OR country = :country) " +
       "AND (:language = '' OR language = :language) " +
       "AND (:keyword = '' OR name LIKE '%' || :keyword || '%' OR tags LIKE '%' || :keyword || '%') " +
       "ORDER BY clickcount DESC")
List<RadioStation> getFilteredStations(String country, String language, String keyword);
```

### 4.4 保存文件（SAF）

```java
private static final int REQUEST_SAVE_FILE = 1;
private List<RadioStation> stationsToExport;
private int exportFormat;

private void startExport(List<RadioStation> stations, int format) {
    this.stationsToExport = stations;
    this.exportFormat = format;
    Intent intent = new Intent(Intent.ACTION_CREATE_DOCUMENT);
    intent.addCategory(Intent.CATEGORY_OPENABLE);
    switch (format) {
        case 0: // M3U
            intent.setType("audio/x-mpegurl");
            intent.putExtra(Intent.EXTRA_TITLE, "stations.m3u");
            break;
        case 1: // CSV
            intent.setType("text/csv");
            intent.putExtra(Intent.EXTRA_TITLE, "stations.csv");
            break;
        case 2: // JSON
            intent.setType("application/json");
            intent.putExtra(Intent.EXTRA_TITLE, "stations.json");
            break;
    }
    startActivityForResult(intent, REQUEST_SAVE_FILE);
}

@Override
protected void onActivityResult(int requestCode, int resultCode, @Nullable Intent data) {
    if (requestCode == REQUEST_SAVE_FILE && resultCode == RESULT_OK && data != null) {
        Uri uri = data.getData();
        if (uri != null) {
            exportToUri(uri, stationsToExport, exportFormat);
        }
    }
}

private void exportToUri(Uri uri, List<RadioStation> stations, int format) {
    new Thread(() -> {
        try (OutputStream os = getContentResolver().openOutputStream(uri)) {
            if (os == null) throw new IOException("无法打开输出流");
            switch (format) {
                case 0: ExportHelper.exportToM3U(stations, os); break;
                case 1: ExportHelper.exportToCSV(stations, os); break;
                case 2: ExportHelper.exportToJSON(stations, os); break;
            }
            runOnUiThread(() -> Toast.makeText(this, "导出成功", Toast.LENGTH_SHORT).show());
        } catch (IOException e) {
            runOnUiThread(() -> Toast.makeText(this, "导出失败：" + e.getMessage(), Toast.LENGTH_LONG).show());
        }
    }).start();
}
```

***

## 5. 测试与验证

| 测试项        | 预期结果                       |
| :--------- | :------------------------- |
| 输入无效服务器地址  | 同步失败并显示错误信息                |
| 输入有效服务器地址  | 能够下载全部电台数据，进度条正常更新         |
| 同步完成后查询数据库 | 能够根据国家、语言、关键词筛选出正确结果       |
| 导出 M3U 文件  | 生成的文件能被播放器识别且链接有效          |
| 导出 CSV 文件  | 能用 Excel 打开，数据完整           |
| 导出 JSON 文件 | 格式合法，包含所有字段                |
| 重复同步       | 第二次同步时间更短（因为增量？目前是全覆盖，可接受） |

***

## 6. 打包与分发

- 生成 signed APK 或 AAB
- 版本号自定，例如 1.0
- 应用名称：RadioDBTool
- 发布到 GitHub 或直接安装使用

***

## 7. 后续可选增强

- **增量同步**：记录上次同步时间，只拉取 `lastchangetime` 之后更新的电台。
- **更多导出格式**：PLS, XSPF, 直接复制到剪贴板。
- **在线筛选**：不保存数据库，实时从服务器拉取并筛选导出（适合小批量）。
- **国际化**：支持多语言界面。

***

以上大纲涵盖了从项目创建、代码移植、UI 设计到核心功能实现的所有步骤，按照此大纲即可完成独立工具的开发。如需具体代码文件的详细修改示例，可以进一步提供。
